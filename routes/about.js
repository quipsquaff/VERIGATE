var express = require("express");
var router = express.Router();

// var application_controller = require("../controllers/about");
// var devices_controller = require("../controllers/devices_controller");
// var isAuthenticated = require("../config/middleware/isAuthenticated");

// router.get('/about', isAuthenticated, application_controller.index);

// router.put('/about', isAuthenticated, application_controller.updateSwitch); // Route to UPDATE switchState on the 'gates' table.

// router.put('/', devices_controller.updateSwitch);

module.exports = router;
